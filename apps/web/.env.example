API_URL=http://localhost:4000/api

# Secret used to authenticate cache revalidation requests (POST /api/revalidate).
# Generate with: openssl rand -hex 32
# Token must be sent in the x-revalidate-token header.
REVALIDATE_SECRET=change-me-openssl-rand-hex-32
